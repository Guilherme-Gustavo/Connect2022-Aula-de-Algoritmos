#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i=0,j=0;
    printf("Os valores dos pares [i,j] sao:\n\n");
    for (i=0;i<3; i++)
        for (j=0; j<3; j++)
            printf("[%d,%d]\n",i,j);

    printf("i final eh: %d e j final eh %d \n\n",i,j);

int a;

   /* for loop execution */
   for(int a = 10; a < 20; a = a + 1 ){
      printf("value of a: %d\n", a);
   }
    printf("value of a: %d\n", a);
}

